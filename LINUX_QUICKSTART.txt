═══════════════════════════════════════════════════════════════════
  Basler Vision System - Linux 快速啟動指南
═══════════════════════════════════════════════════════════════════

🚀 快速安裝（Ubuntu/Debian）

1. 安裝依賴：
   sudo apt-get update
   sudo apt-get install -y libxcb-xinerama0 libxcb-cursor0 libxkbcommon-x11-0 libgl1-mesa-glx

2. 進入程式目錄：
   cd usr/bin

3. 添加執行權限：
   chmod +x BaslerVisionSystem

4. 運行應用：
   ./BaslerVisionSystem

───────────────────────────────────────────────────────────────────

📦 其他 Linux 發行版

Fedora/RHEL:
  sudo dnf install -y xcb-util-wm xcb-util-image mesa-libGL

Arch Linux:
  sudo pacman -S libxcb xcb-util-wm mesa

───────────────────────────────────────────────────────────────────

🛠️ 完整安裝選項

如需系統整合（桌面快捷方式、命令行別名等），請參閱：
https://github.com/你的用戶名/專案名/blob/master/LINUX_INSTALL.md

或查看專案中的 LINUX_INSTALL.md 文件（如有包含）

───────────────────────────────────────────────────────────────────

⚠️ 常見問題

問題：提示缺少共享庫
解決：ldd usr/bin/BaslerVisionSystem | grep "not found"
     檢查缺少哪些庫並手動安裝

問題：顯示相關錯誤
解決：export QT_QPA_PLATFORM=xcb
     ./BaslerVisionSystem

───────────────────────────────────────────────────────────────────

© 2025 Industrial Vision. All rights reserved.
